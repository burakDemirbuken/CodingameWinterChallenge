#pragma once

// finish

enum e_way
{
	NORTH,
	EAST,
	SOUTH,
	WEST,
	X
};

enum e_mapTile
{
	WALL,
	ROOT,
	BASIC,
	TENTACLE,
	HARVESTER,
	SPORER,
	A,
	B,
	C,
	D,
	EMPTY
};

enum e_owner
{
	ENEMY,
	PLAYER,
	NEITHER
};
